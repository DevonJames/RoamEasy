import React from "react"; import { View, StyleSheet, ViewProps, ViewStyle } from "react-native"; interface CardProps extends ViewProps { style?: ViewStyle | ViewStyle[]; } const Card: React.FC<CardProps> = ({ children, style, ...props }) => { return ( <View style={[styles.card, style]} {...props}> <View>{children}</View> </View> ); }; const styles = StyleSheet.create({ card: { backgroundColor: "white", borderRadius: 8, shadowColor: "#000", shadowOffset: { width: 0, height: 2 }, shadowOpacity: 0.1, shadowRadius: 4, elevation: 3, margin: 4, }}); export default Card;
